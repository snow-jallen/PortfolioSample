@page "/project"

<h3>Project</h3>


@code {
    [Parameter]
    public int ProjectId{get;set;}

    public Project SelectedProject{get; set;}

    protected override async Task OnInitializedAsync()
    {
        if(ProjectId == 0)
            return;

        //SelectedProject = await repository.GetProjectByIDAsync(ProjectId);
    }
}
